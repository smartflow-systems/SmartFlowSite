## Unreleased

- Added automated Node.js test coverage for the `/gh-sync` Express endpoint (VERIFY: `npm test` • UNDO: `git revert <commit>`).
- Documented how to run the test suite locally with no extra environment setup (VERIFY: `npm test` • UNDO: `git checkout -- README.md`).
