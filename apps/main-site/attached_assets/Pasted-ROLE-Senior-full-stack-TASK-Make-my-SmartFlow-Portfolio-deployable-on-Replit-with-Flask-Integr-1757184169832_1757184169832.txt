ROLE: Senior full-stack.  
TASK: Make my SmartFlow Portfolio deployable on Replit with Flask. Integrate my assets (logo + GIFs + site files) and ensure everything is production-ready in a black + shiny gold premium theme with animated hero visuals.  

INSTRUCTIONS:  

1. Deployment settings:  
   • Run command → python3 main.py  
   • Build command → python3 -m pip install -U pip && python3 -m pip install -r requirements.txt  
   • Port → auto-detect, server must bind to 0.0.0.0 using env PORT  

2. Create/overwrite these files:

--- FILE: requirements.txt ---
flask

--- FILE: server.py ---
import os
from pathlib import Path
from flask import Flask, send_from_directory

def main():
    app = Flask(__name__, static_folder=str(Path(__file__).parent))

    @app.route("/")
    def root():
        return send_from_directory(app.static_folder, "index.html")

    @app.route("/<path:filename>")
    def assets(filename):
        return send_from_directory(app.static_folder, filename)

    port = int(os.getenv("PORT", 3000))
    app.run(host="0.0.0.0", port=port)

if __name__ == "__main__":
    main()

--- FILE: index.html ---
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>SmartFlow Systems — Portfolio</title>
  <link rel="stylesheet" href="styles.css">
  <link rel="icon" href="favicon.png">
</head>
<body>
  <nav class="navbar">
    <img src="static-logo.png" alt="SmartFlow Logo" class="logo">
    <ul>
      <li><a href="#hero">Home</a></li>
      <li><a href="#projects">Projects</a></li>
      <li><a href="#testimonials">Testimonials</a></li>
      <li><a href="#pricing">Pricing</a></li>
    </ul>
  </nav>

  <section id="hero" class="hero">
    <div class="hero-bg">
      <img src="sfs-gif.gif" alt="SmartFlow Animation">
    </div>
    <div class="hero-content">
      <h1>SmartFlow Systems</h1>
      <p>AI Bots • Booking • Ecom • Websites</p>
      <a href="#pricing" class="cta-btn">Get Started</a>
    </div>
  </section>

  <section id="projects" class="projects">
    <h2>Our Systems</h2>
    <div class="card-grid">
      <div class="card">AI Social Bot</div>
      <div class="card">Booking System</div>
      <div class="card">Ecommerce Shops</div>
      <div class="card">Websites</div>
    </div>
  </section>

  <section id="testimonials" class="testimonials">
    <h2>What Clients Say</h2>
    <p>“SmartFlow changed my business.”</p>
  </section>

  <section id="pricing" class="pricing">
    <h2>Plans</h2>
    <div class="plan">Starter</div>
    <div class="plan">Pro</div>
    <div class="plan">Premium</div>
  </section>

  <footer>
    <p>© 2025 SmartFlow Systems</p>
  </footer>
</body>
</html>

--- FILE: styles.css ---
body {
  margin: 0;
  font-family: 'Poppins', sans-serif;
  background: #0b0b0b;
  color: #f5f5f5;
}

.navbar {
  display: flex;
  justify-content: space-between;
  align-items: center;
  background: #1a1a1a;
  padding: 10px 20px;
  border-bottom: 2px solid gold;
}

.logo {
  height: 50px;
}

.navbar ul {
  list-style: none;
  display: flex;
  gap: 20px;
}

.navbar a {
  color: gold;
  text-decoration: none;
  font-weight: bold;
}

.hero {
  position: relative;
  text-align: center;
  padding: 100px 20px;
  overflow: hidden;
  background: black;
}

.hero-bg img {
  position: absolute;
  top: 50%;
  left: 50%;
  width: 500px;
  max-width: 80%;
  transform: translate(-50%, -50%);
  opacity: 0.2;
  animation: spin 20s linear infinite;
}

.hero-content {
  position: relative;
  z-index: 2;
}

.hero h1 {
  font-size: 3rem;
  color: gold;
}

.cta-btn {
  display: inline-block;
  margin-top: 20px;
  padding: 12px 30px;
  background: gold;
  color: black;
  font-weight: bold;
  text-decoration: none;
  border-radius: 4px;
}

.projects, .testimonials, .pricing {
  padding: 60px 20px;
  text-align: center;
}

.card-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
  gap: 20px;
  margin-top: 20px;
}

.card {
  padding: 20px;
  border: 2px solid gold;
  background: #1a1a1a;
  color: gold;
  font-weight: bold;
  border-radius: 6px;
}

.plan {
  display: inline-block;
  margin: 10px;
  padding: 20px;
  border: 2px solid gold;
  background: #1a1a1a;
  color: gold;
  border-radius: 6px;
  font-weight: bold;
}

footer {
  background: #0b0b0b;
  text-align: center;
  padding: 20px;
  border-top: 2px solid gold;
  color: #aaa;
}

@keyframes spin {
  from { transform: translate(-50%, -50%) rotate(0deg); }
  to { transform: translate(-50%, -50%) rotate(360deg); }
}

3. Keep main.py exactly as already written (entrypoint with deployment checks).  

4. Place assets:  
   • static-logo.png → root  
   • favicon.png → /assets/  
   • sfs-gif.gif (your gold spinning gif) → root  

OUTPUT: A ready-to-deploy SmartFlow portfolio with black background, shiny gold accents, logo, and animated hero background.  
