# Prod hardening examples
DEBUG=0
# Only trust proxy headers from these IPs (comma-separated)
FORWARDED_ALLOW_IPS=127.0.0.1
# App should enforce max request size (bytes)
WEB_MAX_CONTENT_LENGTH=10485760
