// Given the file structure provided, insert a basic Prisma schema example for a User model.

// This may typically include fields such as id, name, email, and createdAt.
model User {
  id        Int      @id @default(autoincrement())
  name      String
  email     String   @unique
  createdAt DateTime @default(now())
}