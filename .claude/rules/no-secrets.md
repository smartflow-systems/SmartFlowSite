# Rule: No Secrets

Never open, print, copy, summarise, or store secret values.

Do not open or read:

- `.env` or `.env.*`
- credentials, tokens, private keys, API keys
- database connection strings or URIs
- cookies or session data
- production logs containing sensitive data
- customer data, billing data, or lead records

Secret names are acceptable context. Secret values are never acceptable.
