name: sfs-ci-deploy
on:
  push:
    branches: ['**']
  pull_request:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '20' }
      - run: npm ci || pnpm i --frozen-lockfile || yarn install --frozen-lockfile
      - run: npm run -s build || echo "no build script; skipping"
  # TODO: swap to reusable workflow when ready:
  # deploy:
  #   uses: smartflow-systems/SmartFlowSite/.github/workflows/sfs-ci-deploy.yml@main
  #   secrets: inherit
